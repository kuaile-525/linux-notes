# Week01

## 9.19虚拟机环境与三种网络模式

> 这是我读韩顺平 Linux 课程 P1–P10 的笔记。  
> 这一节讲的不是命令，是“装环境 + 配网络”，是后面所有实操的地基。  
> 对网安/运维岗位的招聘方来说，这一节展示的是：**理解虚拟化、能在隔离环境里安全做实验**。

#### 重点

> \*\*“Linux 最重要的是‘服务器’！”

#### 一、三种网络连接方式（面试常客）

##### 1. 桥接模式

- 虚拟系统可以和外部系统通讯
- **缺点：容易造成 IP 冲突**
  - 例子：张三和李四主机都在 192.168.0.0–80 这个网段，  
    他俩都选桥接模式，IP 就可能撞上
  - 200 个人同时桥接就更乱

##### 2. NAT 模式（Network Address Translation）

- 虚拟系统可以和外部系统通讯，**不造成 IP 冲突**
- 内网到外网的通信成立，**外网到内网不成立**
- 主机的虚拟网卡和虚拟系统在同一个网段，例如：
  - 虚拟网卡：192.168.100.99
  - 虚拟系统：192.168.100.88

##### 3. 主机模式

- 独立的系统，不与外界发生联系

---

#### 二、虚拟机克隆

- **方式一**：直接拷贝一份安装好的虚拟机文件
- **方式二**：使用 VMware 的克隆操作
  - 注意：克隆时需要**先关闭 Linux**

---

#### 三、虚拟机快照

> 如果你在使用虚拟机系统（比如 Linux）时，担心误操作造成系统异常，  
> 需要回到原先某个正常运行的状态，VMware 也提供了这样的功能，就叫快照管理。

##### 操作演示步骤

1. 安装好系统以后，先做一个**快照 A**（系统开机创建快照，关机也行但弱于开机）
2. 进入系统，创建一个文件夹，再保存一个**快照 B**
3. 回到系统刚刚安装好的状态，即 **快照 A**
4. 试试看，是否还能再次回到**快照 B**——能

> 这一节对我的启发：**养成“动手前先存档”的习惯**。  
> 学运维或渗透，每改一个重要配置前先快照，能省无数次回滚和重装的时间。

---

#### 四、虚拟机迁移与删除

> 我在前面讲过，虚拟系统安装好了，它的本质就是文件（放在文件夹里的）。  
> 因此虚拟系统的迁移很方便：把安装好的虚拟系统这个文件夹整体拷贝或剪切到另外位置使用。  
> 删除也很简单，用 VMware 进行移除，再点击菜单“从磁盘删除”即可，  
> 或者直接手动删除虚拟系统对应的文件夹即可。

---

#### 周复盘

**本周为概念周，没有具体命令要敲。** 下面四件事是我能用自己话讲出来的：

- [ ] 三种网络模式的区别（桥接 / NAT / 主机）
- [ ] 克隆前的关机要求
- [ ] 快照的工作原理（存档 vs 恢复，方向不要搞反）
- [ ] 虚拟机本质 = 文件，迁移 = 拷文件夹
- [ ] 高频五件套一句话：/etc 配置、/var 日志、/usr 程序、/dev 设备、/proc 信息
- [ ] CentOS 与 Ubuntu 的包管理差异（yum/rpm vs apt/dpkg）
- [ ] 远程连接：ssh 登录、scp 传文件（不装第三方工具）

## 9.20

### 目录结构介绍

![image-20260920135119194](week01-目录结构与文件操作%20.assets/image-20260920135119194.png)

linux会把硬件映射成文件进行处理

##### 小疑惑：为什么 /dev 里找不到 cpu？

CPU 不是"可读写的设备"，是计算单元，所以它不进 /dev，而是以**信息文件**的形式住在 /proc 里（`/proc/cpuinfo`）。`/dev` 里其实**可以**有一个 `cpu` 目录（如 `/dev/cpu/0/msr`），但那是给底层调试用的（读写 CPU 寄存器），需要手动加载 `msr` 内核模块才会出现——平时没有是**完全正常**的。（备注：渗透测试里有 MSR 相关攻击面，Spectre 类漏洞利用会碰它，学到内核安全时再见。）

![image-20260920135908493](week01-目录结构与文件操作%20.assets/image-20260920135908493.png)

#### 常见命令

```bash
# CPU 相关
nproc                 # 直接告诉你几个核 —— 预期输出：4
lscpu                 # 详细型号、核数、架构，最美观
cat /proc/cpuinfo     # 最原始的完整信息（每个逻辑核一段）

# 目录与文件
ls /                  # 看根目录全家福
ls /etc | head -30    # 翻翻配置区
ls /var/log           # 认认日志文件的脸
cat /etc/passwd       # 看看你机器上有哪些用户（只读，安全）
```

> `cat /etc/passwd` 输出里看到 `用户名:x:1000:1000:...` 这样的行别慌，  
> 这是"用户名:密码占位:UID:组ID"。

#### 根目录 / 下的常驻居民

##### 第一梯队：天天打交道（★★★）

**/etc【常用】★★★**

- 干什么：所有系统管理所需的配置文件和子目录（例：mysql 的 my.conf）
- 运维视角：**你未来的主战场**——改 SSH 配置（/etc/ssh/sshd_config）、配 Nginx、管用户（/etc/passwd、/etc/shadow）、配计划任务（/etc/crontab），全是这里
- 安全视角：**渗透测试头号目标**。/etc/passwd（用户清单）和 /etc/shadow（密码哈希）是提权后的必看文件，面试常问"拿到 shell 后第一个看哪"
- 三系统差异：CentOS 和 Ubuntu/Kali 结构一致，**部分配置文件位置不同**（如网络配置：CentOS 在 /etc/sysconfig/network-scripts/，Ubuntu 在 /etc/netplan/）——这是面试讲发行版差异的好素材

**/var（老师没标红但★★★）**

- 干什么：存放经常变化的文件——日志、邮件、缓存
- 运维视角：**日志全在这**：/var/log/syslog（Ubuntu）/ /var/log/messages（CentOS）、/var/log/auth.log（登录记录）、/var/log/apache2/（Web 日志）
- 安全视角：SOC 的眼睛。**中行 JD 里"日志审计、入侵监测"就靠这目录吃饭**；查爆破攻击第一步 `grep "Failed password" /var/log/auth.log`
- 差异：日志文件名 CentOS 和 Ubuntu 不同（messages vs syslog），概念一致

**/home【常用】★★★**

- 干什么：普通用户的主目录，每个用户一个以账号命名的文件夹
- 运维视角：磁盘配额（quota）管理的地方；新员工入职建账号就是在 /home 下多一个文件夹
- 安全视角：排查入侵时翻可疑用户的家目录（.bash_history、.ssh/authorized_keys 是后门常客）
- 注意：**/root 不在 /home 里**，它是独立的（见下）

**/root【常用】★★★**

- 干什么：系统管理员（超级权限者）的用户主目录
- 安全视角：提权的终点站。攻击者拿到 root 后第一件事常是翻 /root/.bash_history 看管理员敲过什么
- 易混点（面试常挖坑）：**/ 是根目录，/root 是 root 用户的家**——两个概念完全不同

##### 第二梯队：常用但要分清（★★）

**/bin【常用】→【不推荐/知道即可】★★**

- 干什么：Binary 缩写，最经常使用的命令（ls、cp、cat）
- 老师原话：CentOS 7 后 /bin 是 /usr/bin 的软链接（快捷方式）（图上 "/usr/bin、/usr/local/bin"）
- 运维视角：知道"命令其实是文件、住在 /usr/bin"即可——`which ls` 可以验证
- 差异：**Ubuntu/Kali 上 /bin → /usr/bin 是确定的软链接**；这个问题在 CentOS 7 前后才统一

**/sbin（/usr/sbin）★★**

- 干什么：s = Super user，系统管理员使用的管理程序（shutdown、fdisk、iptables）
- 安全视角：**排查后门时对比 /sbin 下文件的修改时间**——攻击者替换系统命令是持久化手段之一

**/usr【常用】★★★**

- 干什么：非常重要的目录，用户的很多应用程序和文件放这，类似 Windows 的 Program Files
- 运维视角：apt/yum 装的软件基本都在这；/usr/local 是你手动编译安装软件的标准位置
- 记法：**装东西看 /usr，改配置看 /etc——运维两大高频目录**

**/usr/local【常用】**

- 干什么：**手动安装软件的目录**——区别于包管理器：`apt/yum` 装的进 /usr，**你自己编译源码 / 手动解压 tar.gz 安装的进 /usr/local**
- 运维视角：企业里编译安装 Nginx、新版 OpenSSL、自研工具的标准位置。为什么要手动编译？——**包管理器里的版本常常太旧，生产环境需要新版本或自定义编译参数**（面试常问"apt 装的和编译装的有什么区别"）
- 老师视角里这是"给主机额外安装软件"的另一个位置（和 /opt 并列），分界习惯：**/opt 放大型商业软件整包，/usr/local 放编译安装的开源软件**
- 安全视角：排查入侵时 `ls -lt /usr/local/bin | head`——攻击者编译恶意工具常落在这，看修改时间找异常
- 三系统一致：CentOS / Ubuntu / Kali 这个目录用法完全一样，没有差异，放心记

**/selinux【security-enhanced linux】★★（你的方向重点）**

- 干什么：SELinux 是一种**安全子系统**，能控制"程序只能访问特定文件"（强制访问控制 MAC），有三种工作模式可自行设置
- 三种模式（面试可能考）：
  - **Enforcing**（强制）：违规访问直接拦截并记日志
  - **Permissive**（宽容）：不拦截，只记日志——排查"是不是 SELinux 搞的鬼"时用
  - **Disabled**（关闭）
- 关键差异：**CentOS 默认装 SELinux；Ubuntu/Kali 默认用另一套叫 AppArmor，根本没有 /selinux 目录**——所以你 `ls /` 找不到它不是你装错了，是发行版安全模块不同
- 为什么对你的方向重要：
  1. 面试问"Linux 有哪些安全加固手段"，SELinux/AppArmor 是标准答案之一
  2. 运维事故经典锅：服务起不来、文件明明 755 却读不了 → 十有八九是 SELinux 拦的（`getenforce` 一查便知）——CentOS 服务器运维必备排查项
  3. 安全岗加分认知：**强制访问控制（MAC）vs 普通权限（DAC）**——chmod 那套是"主人自己定权限"（DAC），SELinux 是"系统强制规定程序能碰什么"（MAC），哪怕程序被攻破也跑不出沙盒。**这就是"纵深防御"思想的落地实现**
- 常用命令（以后在 CentOS 上会用到）：`getenforce`（看模式）、`setenforce 0`（临时切宽容）、配置文件 /etc/selinux/config

**/boot【常用】★★**

- 干什么：启动 Linux 使用的核心文件，包括连接文件和镜像文件（vmlinuz、grub）
- 安全视角：grub 被篡改 = 系统启动被劫持；一般不动它
- 运维视角：清理旧内核时才进来（`apt autoremove` 自动干）

##### 第三梯队：设备与虚拟文件系统（要懂原理 ★★）

**/dev ★★**

- 干什么：类似 Windows 的设备管理器，把所有硬件用文件形式存储（sda 磁盘、tty 终端、null、random）
- 运维视角：挂磁盘、看分区（/dev/sda1）天天见
- 你已踩过的坑：**CPU 不在 /dev**（在 /proc/cpuinfo）；/dev/cpu/0/msr 要加载 msr 模块才出现
- 安全视角：/dev/shm（内存文件系统）是攻击者爱放的落地点——重启即消失，取证时要看

**/proc~~【不能动】~~★★**

- 干什么：虚拟目录，系统内存的映射，访问它获取系统信息（/proc/cpuinfo、/proc/meminfo、/proc/数字=每个进程）
- 运维视角：`cat /proc/cpuinfo`、`ps` 命令的本质就是读 /proc
- 安全视角：**恶意软件藏身处**——`ls /proc` 里 PID 对不上进程列表 = 有内核级后门（rootkit 排查思路）
- 为什么"不能动"：它是内存映射不是真文件，改了也没意义还可能搞崩

**/sys~~【不能动】~~★**

- 干什么：2.6 内核后出现的新虚拟文件系统（sysfs），把硬件和内核参数暴露出来
- 定位：/proc 的现代化分身，日常用得少，知道"也是虚拟的、别动"即可

##### 第四梯队：挂载与可选（会用就行 ★★）

**/media【常用】★**

- 干什么：Linux 自动识别设备（U 盘、光驱）后自动挂载到这
- 差异：桌面版（Kali）插 U 盘自动挂到 /media/用户名/；**服务器版（你的 Ubuntu Server）通常要手动 mount**——又是桌面版 vs 服务器版的经典差异

**/mnt【常用】★**

- 干什么：系统提供让用户**临时挂载**别的文件系统（老师例：把外部存储挂到 /mnt/myshare）
- 运维视角：临时接 NFS 共享、挂新盘测试都用这；习惯是"临时用 /mnt，长期用 /etc/fstab 配置"

**/opt ★**

- 干什么：给主机**额外安装软件**所存放的目录（例：装 Oracle）。默认为空
- 运维视角：企业里大型商业软件（Oracle、WebLogic）和不少自研服务习惯装 /opt/软件名——实习进公司大概率见到
- Kali 视角：部分渗透工具（如某些商业版 burp）也装 /opt

##### 第五梯队：知道即可（★）

**/srv ★**~~（不能动）~~

- 干什么：service 缩写，存放服务启动之后需要提取的数据（如 Web 服务的页面文件）
- 实际：很多发行版默认是空的（Ubuntu 的 Nginx 页面在 /var/www/html 而不是 /srv），**知道概念即可**

**/tmp ★**

- 干什么：存放临时文件，重启可能清空，所有用户可写（权限 1777）
- 安全视角：**攻防双面人**——攻击者的经典落地目录（无权限门槛），取证必查；面试问"/tmp 为什么危险"答：任何人可写 + 可执行位历史问题 = 提权跳板

**/lib ★**

- 干什么：系统开机所需最基本的动态连接共享库，类似 Windows 的 DLL，几乎所有应用程序都要用
- 知道即可：删了系统就废，一般不碰

**/lost+found（不推荐）**

- 干什么：一般情况下空，**非法关机后**这里存放一些碎片文件
- 知道即可： ext4 文件系统的自愈产物，每个挂载点独有一个

---

##### 三、CentOS（视频）vs 你的 Ubuntu/Kali 对照表

| 项目              | CentOS（老师演示）                    | Ubuntu（你的服务器）     | Kali（你的攻击机）              |
| --------------- | ------------------------------- | ----------------- | ------------------------ |
| 包管理             | yum / dnf、rpm                   | apt / dpkg        | apt / dpkg               |
| 系统日志            | /var/log/messages               | /var/log/syslog   | /var/log/syslog          |
| 登录日志            | /var/log/secure                 | /var/log/auth.log | /var/log/auth.log        |
| 网络配置            | /etc/sysconfig/network-scripts/ | /etc/netplan/     | /etc/NetworkManager/（桌面） |
| /bin 与 /usr/bin | CentOS 7 起合并                    | 一直是软链接            | 一直是软链接                   |
| U 盘自动挂载         | 桌面环境才有                          | Server 无桌面需手动     | 有桌面自动挂 /media            |
| 安全子系统           | **SELinux**（有 /selinux）         | **AppArmor**（无）   | **AppArmor**（无）          |
| 软件默认位置          | /usr + /opt（商业软件）               | /usr              | /usr（工具齐全）               |

#### 对你方向最重要的三个

1. **/etc** —— 运维之魂。以后配 SSH、配 Nginx、改用户密码策略，全在 `/etc` 里翻文件。记住子目录 `/etc/passwd`（用户清单）和 `/etc/shadow`（密码哈希）——**这两个文件是渗透测试的头号目标**，网安面试必考。
2. **/var/log** —— SOC 之眼。你第 4 周写日志巡检脚本、以后做蓝队，每天泡的地方：`/var/log/auth.log`（登录记录，看有没有人爆破你）、`/var/log/apache2/`（Web 访问记录）。
3. **/tmp** —— 攻防双面人：任何用户都能写，权限 1777。攻击者爱在这放临时脚本，防守者爱在这翻可疑文件。

#### 两条 Linux 世界观

1. **没有盘符**。Windows 是 C 盘 D 盘，Linux 是**一棵树**：`/` 是树根，一切目录都是树枝，U 盘插上来也是“挂载”成树枝上的一个目录（`/media/xxx`），不是多一个盘。
2. **/root ≠ /**。`/` 是整棵树的根；`/root` 只是 root 用户的家目录，是树枝中的一根。新手最常混的一对。

### Linux 远程控制（9.20 补充）

**结论：不装 Xshell / Xftp / MobaXterm——Windows 自带 ssh / scp 全覆盖，少装一个工具少一分折腾。**

| 场景        | 用什么            | 命令                                |
| --------- | -------------- | --------------------------------- |
| 远程登录服务器   | Windows 自带 ssh | `ssh 用户名@192.168.x.x`             |
| 传文件到服务器   | Windows 自带 scp | `scp D:\文件.txt 用户名@IP:/home/用户名/` |
| 从服务器拿文件下来 | 同上，方向反过来       | `scp 用户名@IP:/var/log/xxx.log D:\` |

#### 补充：kali linux固定IP

1. 屏幕左上角（或左下角）打开**应用程序菜单** → 找 **设置（Settings）**；
2. 左侧栏点 **网络（Network）**；
3. 点 **有线连接 1** 旁边的**齿轮图标 ⚙️**；
4. 切到 **IPv4** 标签 → 方法从“自动(DHCP)”改成 **手动（Manual）**；
5. 地址栏填：IP / 子网掩码 24 / 网关 / DNS → **应用** → 把有线连接关掉再打开（开关切一下）

## 9.21

### vim

#### vi和vim的基本介绍

Linux 系统会内置 vi 文本编辑器

Vim 具有程序编辑的能力,可以看做是Vi的增强版本,可以主动的以字体颜色辨别语法的正确性,  
方便程序设计。代码补完、编译及错误跳转等方便编程的功能特别丰富,在程序员中被广泛使用。

#### vi和vim常用三种模式

###### 正常模式

以vim打开一个档案就直接进入一般模式了(这是默认的模式)。在这模式中,『删除字符』或『删除整行』来处理档案内容,『复制、粘贴』来处理文件数据。

###### 插入模式

按下i,I,o,O,a,A,r,R等任何一个字母之后才会进入编辑模式,一般来说按i即可.

###### ●命令行模式

输入   ：wq      :q      :q!  :也可以是/

在这个模式当中,可以提供你相关指令,完成读取、存盘、替换、离开vim、显示行号等的动作  则是在此模式中达成的!

###### 示例

```bash
vim hello.java         #回车
#按键i                    #插入模式
#编辑java文件
#按键esc          #退出
:wq          #命令行模式+write

```

![image-20260921202108477](week01-目录结构与文件操作%20.assets/image-20260921202108477.png)

#### vim快捷键

> 为什么这一节单独列：远程服务器上没有 IDE，**以后改 SSH 配置 / Nginx 站点 / Wazuh 规则，全靠 vim**——这是 W4-W21 每天会用的高频工具。

| # | 模式    | 快捷键                     | 作用                             |
| - | ----- | ----------------------- | ------------------------------ |
| 1 | 一般模式  | `yy` / `5yy`            | 复制当前行 / 当前行向下 5 行；`p` 粘贴到光标下一行 |
| 2 | 一般模式  | `dd` / `5dd`            | 删除当前行 / 当前行向下 5 行（不含本行）        |
| 3 | 命令行模式 | `/关键字` + Enter          | 向下查找；`n` 跳下一个匹配                |
| 4 | 命令行模式 | `:set nu` / `:set nonu` | 显示 / 取消行号                      |
| 5 | 一般模式  | `G` / `gg`              | 跳到文件末行 / 首行                    |
| 6 | 一般模式  | `u`                     | 撤销动作                           |
| 7 | 一般模式  | `20` + `Shift+g`        | 跳到第 20 行（一般文件也成立）              |
| 8 | —     | 自查                      | 更多快捷键自行搜索（`:help`）             |

5和7视频举例为vim etc/profile  ，的文件，而验证一般文件也可以

### 关机重启  用户登录注销

#### 关机重启

shutdown -h now          立该进行关机  
shutdown  -h  1          “hello,1分钟后会关机了”  
shutdown  -r  now    现在重新启动计算机  
halt                            关机  
reboot                     重启  
sync                           把内存的数据同步到磁盘.

● 注意细节

1. 不管是重启系统还是关闭系统,首先要运行sync命令,把内存中的数据写到磁盘中
2. 目前的shutdown/reboot/halt 等命令均已经在关机前进行了sync,但仍要注意

#### 用户登录与注销

1. 登录时尽量少用root帐号登录,因为它是系统管理员,最大的权限,避免操作失误。可以利用  
   普通用户登录,登录后再用”Su-用户名’命令来切换成系统管理员身份.
2. 在提示符下输入logout 即可注销用户

● 使用细节

logout 注销指令在图形运行级别无效,在<u>运行级别3</u>（无图形化界面）下有效.

> 速记：**3 = 黑框（命令行），5 = 桌面（GUI）**。你的 Ubuntu Server 默认就在级别 3（没有桌面、没有 logout 的"图形层"概念），所以 logout 命令直接登出；Kali 桌面版默认级别 5（有图形层），所以 logout 会从图形会话退出而不是关终端。

```bash
su -root #进入root
logout   #退回用户（注销）
logout   #退出系统了
```

### 9.21 总结

**今天三件事**：

1. vim 三种模式（一般 / 插入 / 命令行）——重点是**模式之间怎么切**（`i` 进插入，`Esc` 回一般，`:wq` 保存退出）
2. vim 8 条常用快捷键——日常改配置会用
3. 关机重启（shutdown / reboot）+ 登录注销（logout）——**运行级别 3=黑框 / 5=桌面**这句要记住

**学完能干什么**：

- 远程登服务器后改配置文件（vim `/etc/ssh/sshd_config` → `i` 进入编辑 → 改完 `Esc` → `:wq`）

  

